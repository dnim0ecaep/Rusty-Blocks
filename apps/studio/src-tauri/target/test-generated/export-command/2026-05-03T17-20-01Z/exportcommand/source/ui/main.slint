import { Button, LineEdit } from "std-widgets.slint";

export component MainWindow inherits Window {
    width: 1024px;
    height: 768px;
    title: "ExportCommand";

    VerticalLayout {
        Button { text: "Add"; }
    }
}
